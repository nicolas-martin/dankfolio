import { StyleSheet } from 'react-native';

export const createDonutStyles = () => StyleSheet.create({
	chartContainer: {
		height: 400,
		padding: 25,
		position: "relative",
	},
	container: {
		flex: 1,
	},
});
