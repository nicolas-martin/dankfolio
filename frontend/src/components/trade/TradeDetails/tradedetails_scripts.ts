export const formatExchangeRate = (rate: string): string => `Rate: ${rate}`;
export const formatGasFee = (fee: string): string => `Network Fee: ${fee} SOL`;
export const formatSpread = (spread: string): string => `Price Impact: ${spread}%`;
