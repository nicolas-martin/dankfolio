appId: com.nicolasmartin.dankfolio
---
- assertVisible:
      id: "horizontal-ticker-card-${COIN_ADDRESS}"

- assertVisible:
      id: "horizontal-ticker-symbol-${COIN_ADDRESS}"

- assertVisible:
      id: "horizontal-ticker-time-${COIN_ADDRESS}"

- assertVisible:
      id: "horizontal-ticker-icon-${COIN_ADDRESS}"
