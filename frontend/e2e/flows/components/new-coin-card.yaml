appId: com.nicolasmartin.dankfolio
---
- assertVisible:
      id: "new-listing-${COIN_SYMBOL}"

- assertVisible:
      id: "new-listing-symbol-${COIN_SYMBOL}"

- assertVisible:
      id: "new-listing-icon-${COIN_SYMBOL}"
