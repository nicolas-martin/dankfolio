jppId: com.nicolasmartin.dankfolio
---
- assertVisible:
      id: "new-coin-card-${COIN_ADDRESS}"

- assertVisible:
      id: "new-coin-symbol-${COIN_ADDRESS}"

- assertVisible:
      id: "new-coin-time-${COIN_ADDRESS}"

- assertVisible:
      id: "new-coin-icon-${COIN_ADDRESS}"
